AddPackages←{
⍝ ⍺: NuGetConsume project namespace
⍝ ⍵: Single package spec or list of package specs
⍝ 'p1'
⍝ 'p1' 'p2'
⍝ ('p1' 'ver')('p2')
⍝    pkgs←⊂¨@(1=≡¨)⊢⍵
    str←(⊣,' --version ',⊢)/⊆⍵
    AP←{⎕CMD'dotnet add ',⍺,' package ',⍵}
    ⍺.project_dir∘AP¨str
    ⍺.packages,←⍵
    ⍺
}
