GetDependencies←{
⍝ ⍵: simple char vec :: path to dependencies configuration file
⍝ ←: table of package names and versions
    json←(⎕JSON ⎕OPT'Dialect' 'JSON5')⊃⎕NGET ⍵
    key←json.packages.⎕NL ¯2
    pkg←JSONDemangle¨key
    ver←json.packages∘⍎¨key
    ⍉↑pkg ver
}
